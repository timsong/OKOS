@model WFS.WebSite4.Areas.Admin.Models.VendorListViewModel
@foreach (var item in Model.Vendors) {
    <div class="panel">
        <div class="row">
            <div class="twelve columns">
                <div class="eight columns">
                    @item.Name
                </div>
                <div class="one columns">
                    @Html.RouteLink("Edit/View","admin.vendor.display",new {vendorID = item.OrganizationId })
                </div>
                <div class="one columns">
					<a href="javascript:void(0)" msclick="vendor.del" msname="@item.Name" msid="@item.OrganizationId">Delete</a>
                </div>
                <div class="one columns"></div>
            </div>
        </div>
    </div>
}
